#include "main.h"
/**
 * main - void
 * _putchar to print function
 * Return: always 0
*/

int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
		_putchar(ch);
	_putchar('\n');

	return (0);
}
